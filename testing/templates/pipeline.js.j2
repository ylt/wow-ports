// Auto-generated by testing/generate-tests.py — DO NOT EDIT
'use strict';
const { test, describe } = require('node:test');
const assert = require('node:assert/strict');
const Pipeline = require('../lib/Pipeline');

{% for section_name, section_tests in sections %}
describe('{{ section_name }}', () => {
  {% for t in section_tests %}
    test('{{ t.id }}: {{ t.description | js_escape }}', () => {
    {% if t.type == 'prefix_encoding' %}
        const enc = Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data: {{ render_input(t.data, 'js', module) }}, metadata: null });
        assert.ok(enc.startsWith({{ render_string(t.expected_prefix, 'js') }}), `Expected {{ t.expected_prefix }} prefix, got: ${enc.slice(0, 10)}`);
      {% if t.not_prefix is defined %}
        {% for np in t.not_prefix %}
        assert.ok(!enc.startsWith({{ render_string(np, 'js') }}));
        {% endfor %}
      {% endif %}
    {% elif t.type == 'prefix_no_prefix' %}
        const enc = Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data: {{ render_input(t.data, 'js', module) }}, metadata: null });
        assert.ok(!enc.startsWith('!'));
    {% elif t.type == 'prefix_detection' %}
        const enc = Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data: {{ render_input(t.data, 'js', module) }}, metadata: null });
        const dec = Pipeline.decode(enc);
        assert.strictEqual(dec.addon, '{{ t.addon }}');
        assert.strictEqual(dec.version, {{ t.version }});
    {% elif t.type == 'export_result_structure' %}
        const enc = Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data: {{ render_input(t.data, 'js', module) }}, metadata: null });
        const dec = Pipeline.decode(enc);
        assert.ok('addon'    in dec, 'missing addon');
        assert.ok('version'  in dec, 'missing version');
        assert.ok('data'     in dec, 'missing data');
        assert.ok('metadata' in dec, 'missing metadata');
    {% elif t.type == 'roundtrip' %}
        const orig = { addon: '{{ t.addon }}', version: {{ t.version }}, data: {{ render_input(t.data, 'js', module) }}, metadata: null };
        const dec = Pipeline.decode(Pipeline.encode(orig));
      {% if t.check_data is string %}
        assert.strictEqual(dec.data, {{ render_string(t.check_data, 'js') }});
      {% endif %}
        assert.strictEqual(dec.addon, '{{ t.check_addon }}');
        assert.strictEqual(dec.version, {{ t.check_version }});
      {% if t.check_metadata_null is defined and t.check_metadata_null %}
        assert.strictEqual(dec.metadata, null);
      {% endif %}
    {% elif t.type == 'roundtrip_table' %}
        const data = {{ render_input(t.data, 'js', module) }};
        const dec = Pipeline.decode(Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data, metadata: null }));
      {% for key, val in t.check_keys.items() %}
        assert.deepStrictEqual({{ render_check_key_access('dec.data', key, 'js') }}, {{ render_input(val, 'js', module) }});
      {% endfor %}
    {% elif t.type == 'roundtrip_nested' %}
        const data = {{ render_input(t.data, 'js', module) }};
        const dec = Pipeline.decode(Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data, metadata: null }));
        assert.strictEqual(dec.data.outer.inner, {{ t.check_outer_inner }});
        assert.deepStrictEqual(dec.data.list, {{ render_input(t.check_list, 'js', module) }});
    {% elif t.type == 'roundtrip_array' %}
        const data = {{ render_input(t.data, 'js', module) }};
        const dec = Pipeline.decode(Pipeline.encode({ addon: '{{ t.addon }}', version: {{ t.version }}, data, metadata: null }));
        assert.deepStrictEqual(dec.data, data);
    {% elif t.type == 'elvui_metadata' %}
        const metadata = { profileType: '{{ t.metadata.profile_type }}', profileKey: '{{ t.metadata.profile_key }}' };
        const orig = { addon: 'elvui', version: 1, data: {{ render_input(t.data, 'js', module) }}, metadata };
        const dec = Pipeline.decode(Pipeline.encode(orig));
        assert.strictEqual(dec.addon, 'elvui');
        assert.deepStrictEqual(dec.metadata, metadata);
      {% for key, val in t.data.items() %}
        assert.strictEqual(dec.data.{{ key }}, {{ render_input(val, 'js', module) }});
      {% endfor %}
    {% elif t.type == 'elvui_no_metadata' %}
        const orig = { addon: 'elvui', version: 1, data: {{ render_input(t.data, 'js', module) }}, metadata: null };
        const dec = Pipeline.decode(Pipeline.encode(orig));
        assert.strictEqual(dec.metadata, null);
    {% endif %}
    });
  {% endfor %}
});

{% endfor %}
